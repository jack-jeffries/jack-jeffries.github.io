\documentclass[12pt]{amsart}


\usepackage{times}
\usepackage[margin=.9 in]{geometry}
\usepackage{paralist,amsmath,amssymb,multicol,graphicx,framed,ifthen,color,xcolor,stmaryrd,enumitem,colonequals}
\usepackage[outline]{contour}
\contourlength{.4pt}
\contournumber{10}
\newcommand{\Bold}[1]{\contour{black}{#1}}

\definecolor{chianti}{rgb}{0.6,0,0}
\definecolor{meretale}{rgb}{0,0,.6}
\definecolor{leaf}{rgb}{0,.35,0}
\newcommand{\Q}{\mathbb{Q}}
\newcommand{\N}{\mathbb{N}}
\newcommand{\Z}{\mathbb{Z}}
\newcommand{\R}{\mathbb{R}}
\newcommand{\C}{\mathbb{C}}
\newcommand{\e}{\varepsilon}
\newcommand{\Gal}{\mathrm{Gal}}
\newcommand{\inv}{^{-1}}
\newcommand{\dabs}[1]{\left| #1 \right|}
\newcommand{\ds}{\displaystyle}
\newcommand{\solution}[1]{\ifthenelse {\equal{\displaysol}{1}} {\begin{framed}{\color{meretale}\noindent #1}\end{framed}} { \ }}
\newcommand{\solutione}[1]{\ifthenelse {\equal{\displaysol}{1}} {\begin{framed}{\color{leaf}This solution is embargoed.}\end{framed}} { \ }}
\newcommand{\showsol}[1]{\def\displaysol{#1}}
\newcommand{\nosolfill}{\ifthenelse {\equal{\displaysol}{1}} {} {\vfill}}
\newcommand{\nosolpage}{\ifthenelse {\equal{\displaysol}{1}} {} {\newpage}}
\newcommand{\nosolonly}[1]{\ifthenelse {\equal{\displaysol}{1}} {} {{#1}}}

\newcommand{\rsa}{\rightsquigarrow}


\newcommand\itemA{\stepcounter{enumi}\item[{\Bold{(\theenumi)}}]}
\newcommand\itemB{\stepcounter{enumi}\item[(\theenumi)]}
\newcommand\itemC{\stepcounter{enumi}\item[{\it{(\theenumi)}}]}
\newcommand\itema{\stepcounter{enumii}\item[{\Bold{(\theenumii)}}]}
\newcommand\itemb{\stepcounter{enumii}\item[(\theenumii)]}
\newcommand\itemc{\stepcounter{enumii}\item[{\it{(\theenumii)}}]}
\newcommand\itemai{\stepcounter{enumiii}\item[{\Bold{(\theenumiii)}}]}
\newcommand\itembi{\stepcounter{enumiii}\item[(\theenumiii)]}
\newcommand\itemci{\stepcounter{enumiii}\item[{\it{(\theenumiii)}}]}
\newcommand\ceq{\colonequals}


\DeclareMathOperator{\ord}{ord}

\DeclareMathOperator{\res}{res}
\setlength\parindent{0pt}
%\usepackage{times}

%\addtolength{\textwidth}{100pt}
%\addtolength{\evensidemargin}{-45pt}\mathrm{Gal}
%\addtolength{\oddsidemargin}{-60pt}

\pagestyle{empty}
%\begin{document}\begin{itemize}

%\thispagestyle{empty}




\begin{document}
\showsol{1}
	
	\thispagestyle{empty}
	
	\section*{Solving polynomials by radicals}
	\begin{framed}
	\textsc{Definition:} Let $F$ be a field, and $f\in F[x]$. We say that $f$ is \textbf{solvable by radicals} if every root of $f$ can be obtained from elements of $F$ by $+$, $-$, $\times$, $\div$, and taking $n$th roots (for any $n>1$).

		\end{framed}

\

	
	\begin{enumerate}
	\itemA Let $F$ be a field of characteristic $\neq 2$. Show that\footnote{Hint: Complete the square!} any quadratic polynomial \[f(x) = ax^2 + bx + c \in F[x]\] with $a\neq 0$ is solvable by radicals.
	
	\solution{We can rewrite as $x^2 + \frac{b}{a} x + \frac{c}{a} = 0$. Then this is the same as $(x+ \frac{b}{2a})^2 +  
	 \frac{c}{a} -  \frac{b^2}{4a^2} = 0$. Then $x+\frac{b}{2a} = \pm \frac{\sqrt{ b^2 - 4ac}}{2a}$ and $x = \frac{-b}{2a} \pm \frac{\sqrt{ b^2 - 4ac}}{2a}$.}
	
	\itemA Solving the cubic: Let $f(x) = x^3 - 3x^2 + 12 x - 7 \in \Q[x]$.
	\begin{enumerate}
	\itema Find a $c\in \Q$ such that, setting $x=y+c$, we have $f(y) = y^3 + p y + q$ with no $y^2$ term.
	\itema Make the \emph{Vieta substitution}\, $y=z-\frac{p}{3z}$, and rewrite the result as a quadratic in $z^3$.
	\itema Without forgetting that any nonzero complex number has three cube roots, solve for $z$, and use this to solve for $x$.
	\itema Discuss: what would you do with a general cubic? Explain why any cubic is solvable by radicals (over $\Q$).
	\end{enumerate}
	
	\solution{ 
		\begin{enumerate}
	\itema Take $c=1$ to get $f(x) = (y+1)^3 - 3 (y+1)^2 + 12 (y+1) - 7 = y^3 + 9y +3$.
	\itema We have $f=(z-\frac{3}{z})^3 + 9(z-\frac{3}{z}) + 3(z-\frac{3}{z}) = z^3 + 3 - \frac{27}{z^3}$. Clearing denominators gives $(z^3)^2 + 3(z^3) - 27 = 0$.
	\itema We have $z^3 = \frac{-3 \pm \sqrt{9-108}}{2} = \frac{3}{2}(-1\pm i \sqrt{11})$. Then the solutions are
	\[ z_j= \omega^j \sqrt[3]{ \frac{3}{2}(-1 \pm i \sqrt{11})}, \quad j=0,1,2\]
	and both choices of $\pm$, and then
	\[ x= 1 + z_j - 9/z_j.\]
	\itema It is the same process for any cubic, so this gives a formula in terms of radicals for the roots.
	\end{enumerate}
	
	
	}
	
	\end{enumerate}
	
	\
	
	
		\begin{framed}
	\textsc{Definition:} Let $G$ be a finite group. We say that $G$ is \textbf{solvable} if there exist subgroups $H_i$ such that \[\{e\} = H_0 \trianglelefteq H_1  \trianglelefteq H_2   \trianglelefteq \cdots  \trianglelefteq H_n = G\] where each consecutive quotient $H_{i+1}/H_i$ is abelian.
	
	
	\
	
		\textsc{Theorem:} Let $F$ be a field of characteristic zero, and $f\in F[x]$ be a separable polynomial. If~$f$ is solvable by radicals, then the Galois group\footnotemark\footnotemark\ of $f$ is a solvable group.
	
	\
	
	
	\textsc{Lemma:} Let $G$ be a finite group. 
	\begin{enumerate}
	\item If $N \trianglelefteq G$, then $G$ is solvable if and only if $N$ and $G/N$ are both solvable.
	\item If $\{e\} = H_0 \trianglelefteq H_1  \trianglelefteq H_2   \trianglelefteq \cdots  \trianglelefteq H_n = G$ and each consecutive quotient $H_{i+1}/H_i$ is \emph{solvable}, then $G$ is solvable.
	\end{enumerate}
		\end{framed}
		\footnotetext[2]{Recall that we have defined the Galois group of a separable polynomial to be the Galois group of its splitting field.} 
				\footnotetext[3]{The converse is also true, but we won't prove it. Moreover, a version of the theorem is true in arbitrary characteristic.} 
		
		\
		
			\begin{enumerate}\setcounter{enumi}{2}
		
		\itemA Not solving the quintic: Let $f(x) = x^5 - 4x -2 \in \Q[x]$.
		\begin{enumerate}
		\itema Show that $f$ is irreducible and has five distinct complex roots, of which three are real\footnote{Hint: Calculus.}.
		\itema Show that\footnote{Hint: Use Cauchy's Theorem and complex conjugation.} the Galois group of $f$ is isomorphic to $\mathcal{S}_5$.
		\itema Use the Theorem to show that $f$ cannot be solved by radicals.
		\end{enumerate}
				\end{enumerate}

%\solution{
%
%\begin{enumerate}\setcounter{enumii}{0}
%		\itema Using Gauss and Eisenstein with $p=2$, we see that $f$ is irreducible. Since $\Q$ is perfect, $f$ is separable, so has $5$ distinct complex roots. We have $f'(x) = 5x^4 - 4$, which is positive on $(-\infty, -\sqrt[4]{4/5}) \cup (\sqrt[4]{4/5},\infty)$  and negative in between, so $f$ has one local max and one local min, so there are at most $3$ real roots. There is a positive value at $x=-1$, a negative value at $x=0$. Using the end behavior and the intermediate value theorem, there are at least three real roots, so exactly three. 
%		\itema Recall that the action of the Galois group on the five roots of $f$ is faithful and transitive, so we can identify the Galois group with a subgroup of $\mathcal{S}_5$. Note that $5$ divides the degree of the splitting field. By Cauchy, there is an element of order $5$ in the Galois group. The only elements of order $5$ in $\mathcal{S}_5$ are $5$-cycles, so the Galois group has a $5$-cycle. Complex conjugation exchanges the two nonreal roots of $f$, so it is a transposition. We have shown earlier that a transposition and a $5$-cycle generate all of $\mathcal{S}_5$. Thus, the Galois group is isomorphic to $\mathcal{S}_5$.
%		\itema Suppose that $f$ can be solved by radicals. By the Theorem, $\mathcal{S}_5$ must then be solvable. By the Lemma, a subgroup of a solvable group is solvable, so $\mathcal{A}_5$ is also solvable. But $\mathcal{A}_5$ is simple, and using the definition it is clear that $\mathcal{A}_5$ then cannot be solvable. This is a contradiction.
%		\end{enumerate}
%
%
%
%}
	
	\nosolpage
	
	\begin{framed}
	\textsc{Proposition:} Let $F$ be a field of characteristic zero.
	\begin{enumerate}
	\item For any $m\geq 1$, the Galois group of $x^m-1$ is abelian.
	\item If $F$ contains all $m$th roots of $1$ and $a\in F$, then the Galois group of $x^m-a$ is cyclic.
	\end{enumerate}
	\end{framed}
	
	\begin{enumerate}\setcounter{enumi}{3}
		\itemA Proof of Proposition $\Longrightarrow$ Theorem: Let $F$ be a field of characteristic zero. Say that a sequence of fields 
\[F=F_0 \subseteq F_1 \subseteq \cdots \subseteq F_n\] is a \textbf{radical tower} if for all $i=0,\dots, n-1$,  $F_{i+1}$ is the splitting field of $x^{m_i}-a_i \in F_i[x]$ for some $m_i \geq 2$ and $a_i \in F_i$.
		\begin{enumerate}
		\itema Suppose that $\beta$ can be obtained from elements of $F$ by $+$, $-$, $\times$, $\div$, and taking $n$th roots. Explain why there exists a radical tower $F=F_0 \subseteq F_1 \subseteq \cdots \subseteq F_n$
such that $\beta\in F_n$.
\itema Suppose that $\beta_1,\dots,\beta_s$ can be obtained from elements of $F$ by $+$, $-$, $\times$, $\div$, and taking $n$th roots. Explain why there exists a radical tower
$F=F_0 \subseteq F_1 \subseteq \cdots \subseteq F_n$
such that $\beta_1,\dots,\beta_s \in F_n$.
\item[]\textsc{Fact:} In the setting of the previous part, one can also arrange that \emph{$F_n/F$ is Galois}.
\itema Suppose that $K/F$ is the splitting field of $x^m-a$ for some $m\geq 2$ and $a\in F$. Show that $\Gal(K/F)$ is solvable.
\itema Let $F=F_0 \subseteq F_1 \subseteq \cdots \subseteq F_n$
be a radical tower with $F_n/F$ Galois. Show that $\mathrm{Gal}(F_n/F)$ is solvable.
\itema Complete the proof of the Theorem.
		\end{enumerate}
	
				
%\solution{
%		\begin{enumerate}
%\itema Consider the sequence of operations used to obtain $\beta$. Until the first root is extracted, we have an element of $F_0$; extracting a root of $a_0\in F_0$ gives an element $\alpha_0$ in the splitting field $F_1$ of $x^{m_0} - a_0$ over $F_0$. Continuing like so, we get the statement.
%\itema By part (a), we can construct a radical tower with $\beta_1\in F_{n_1}$. We can then build a radical tower over $F_{n_1}$ up to $F_{n_2}$ containing $\beta_2$; note that a composition of radical towers is a radical tower. Continuing like so, we get the statement.
%\itema Consider the chain of subgroups $H_i$ corresponding to the fields $F_i$. We have $H_{i+1} = \Gal(F_n / F_{i+1}) \subseteq \Gal(F_n / F_{i})$. Since $F_{i+1}$ is a splitting field of a separable polynomial over $F_i$, it is a Galois extension, so by the Fundamental Theorem corresponds to a normal subgroup. Moreover, the quotient group is $\Gal(F_{i+1}/F_i)$. We can break this extension down into two pieces, by adjoining a roots of unity and then roots of $a_i$. This again gives a factorization of the Galois group into a normal subgroup and a quotient. By the Proposition, both of these are abelian. This shows that $\Gal(F_n/F)$ is solvable.
%\itema Suppose that $f$ is solvable by radicals. Then by part (b), we can embed a splitting field $L$ of $f$ into some $F_n$ at the top of a radical tower, and we can also assume that $F_n/F$ is Galois by the Fact. By part (c), $\Gal(F_n/F)$ is solvable. Since $F\subseteq L \subseteq F_n$ and $L/F$ is Galois, then $\Gal(L/F)$ is a quotient of $\Gal(F_n/F)$ by the Fundamental Theorem. Thus, by the Lemma, $\Gal(L/F)$ is solvable. This completes the proof.
%		\end{enumerate}
%		}
		
			
			\itemA Proof of Proposition (1):
					\begin{enumerate}
\itema Let $F$ be a field of characteristic zero, $m\geq 1$, and $K$ be a splitting field of $x^m-1$. Show that\footnote{Hint: Use a homework problem.} the set $\mu_m \subseteq K$ of solutions of $x^m-1$ in $K$ is a cyclic subgroup of $K^\times$.
\itema Let $\zeta$ be a generator of $\mu_m$. Show that $K=F(\zeta)$.
\itema Show that the map 
\[ \begin{array}{ccc}\mathrm{Gal}(K/F) &\longrightarrow& (\Z/m)^\times \\
\sigma & \mapsto & [i]_m \ \ \ \text{s.t.} \ \sigma(\zeta) = \zeta^i\end{array}\]
is an injective group homomorphism and deduce part (1) of the Proposition.
\end{enumerate}


%\solution{
%		\begin{enumerate}
%
%\itema Recall that we showed that any finite subgroup of the multiplicative group of a field is cyclic. Now, the product of two roots of $x^m-1$ is easily checked to be a root of $x^m-1$, so $\mu_m$ is a subgroup of $K^\times$; moreover there are finitely many. By the exercise, this group is cyclic.
%\itema We can write the roots of $x^m-1$ as $1,\zeta, \dots,\zeta^{m-1}$. Then $K= F(1,\zeta,\dots,\zeta^{m-1}) = F(\zeta)$.
%\itema First, we note that $\sigma(\zeta)$ must be a root of $x^m-1$, so must be $\zeta^i$ for some $i$. Note that since $\zeta^m=1$, we can think of the power of $\zeta$ as an element of $\Z/m$. Let $\sigma,\tau\in \Gal(K/F)$. Set $\sigma(\zeta) = \zeta^i$ and $\tau(\zeta)=\zeta^j$. Then $\sigma \tau(\zeta) = \sigma(\zeta^j) = \sigma(\zeta)^j = (\zeta^i)^j = \zeta^{ij}$. It follows that if $\sigma$ is invertible, then $[i]_m \in (\Z/m)^\times$ and the map given above is a group homomorphism.
%Furthermore, since $\zeta$ generates $K$ over $F$, then  $\sigma$ is determined by the image of $\zeta$; hence the map described is injective.
%
%To deduce the Proposition, we see that $\Gal(K/F)$ is isomorphic to a subgroup of $(\Z/m)^\times$. Since $(\Z/m)^\times$ is abelian, so is $\Gal(K/F)$.
%\end{enumerate}
%}

				
							\itemA Proof of Proposition (2):
					\begin{enumerate}
\itema Let $F$ be a field of characteristic zero containing $m$ distinct $m$th roots of $1$, let $a\in F$, and let $K$ be a splitting field for $x^m-a$. Let $\alpha$ be any root of $x^m-a$. Show that $K = F(\alpha)$.
\itema Let $\zeta$ be a generator for $\mu_m$ as in the previous problem. Show that the map
\[ \begin{array}{ccc}\mathrm{Gal}(K/F) &\longrightarrow& \Z/m \\
\sigma & \mapsto & [i]_m \ \ \ \text{s.t.} \ \sigma(\alpha) = \zeta^i\alpha \end{array}\]
is an injective group homomorphism and deduce part (2) of the Proposition.
\end{enumerate}


%\solution{
%\begin{enumerate}
%\itema Note that for $i=0,\dots,m-1$, we have $(\zeta^i \alpha)^m = \zeta^{im} \alpha_m = a$. Thus, any such $\zeta^i \alpha$  is a root of $x^m-a$. Since $x^m-a$ has at most $m$ roots, this must be every such root. Thus $K=F(\alpha,\zeta \alpha,\dots,\zeta^{m-1}\alpha) = F(\alpha)$.
%\itema First, we note that $\sigma(\alpha)$ must be a root of $x^m-a$, so must be $\zeta^i \alpha$ for some $i=0,\dots,m-1$. Furthermore, since $\alpha$ generates $K$ over $F$, then  $\sigma$ is determined by the image of $\alpha$; hence the map described is injective. To check that it is a group homomorphism, let $\sigma,\tau\in \Gal(K/F)$. Suppose that $\sigma(\alpha)=\zeta^i \alpha$ and $\tau(\alpha) = \zeta^j \alpha$. Then $\sigma \tau (\alpha) = \sigma (\zeta^j \alpha) = \zeta^j \sigma(\alpha) = \zeta^j \zeta^i\alpha = \zeta^{i+j} \alpha$, since $\sigma$ fixes $F$ and $\zeta^j\in F$. Thus $\sigma\tau$ maps to $[i+j]_m$.
%
%To deduce the Proposition, we know that $\mathrm{Gal}(K/F) $ is isomorphic to a subgroup of the cyclic group $\Z/m$, and hence is cyclic.
%\end{enumerate}
%}


			

\end{enumerate}


	
							
\end{document}
